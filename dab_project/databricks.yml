# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: 882c9f8c-8c21-4cd8-9b37-aec0c0e8bec5

include:
  - resources/*.yml
  - resources/*/*.yml

variables:
  catalog:
    default: "citibike_dev"

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-2256718032240687.7.azuredatabricks.net


    presets:
      # Set dynamic_version: true on all artifacts of type "whl".
      # This makes "bundle deploy" add a timestamp to wheel's version before uploading,
      # new wheel takes over the previous installation even if actual wheel version is unchanged.
      # See https://docs.databricks.com/aws/en/dev-tools/bundles/settings
      artifacts_dynamic_version: true

  # devtest:
  #   mode: production
  #   variables:
  #     catalog:
  #       default: "citibike_test"
  #   workspace:
  #     host: https://adb-2256718032240687.7.azuredatabricks.net
  #     # We explicitly deploy to /Workspace/Users/torranceross@ymail.com to make sure we only have a single copy.
  #     root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
  #   presets:
  #     name_prefix: '[devtesting] '
  #   permissions:
  #     - user_name: torranceross@ymail.com
  #       level: CAN_MANAGE
  #     - group_name: users
  #       level: CAN_MANAGE
  test:
    mode: production
    variables:
      catalog:
        default: "citibike_test"
    workspace:
      host: https://adb-2559253398287129.9.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/torranceross@ymail.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    presets:
      name_prefix: '[testing] '
    permissions:
      - user_name: torranceross@ymail.com
        level: CAN_MANAGE
      - group_name: users
        level: CAN_MANAGE
  prod:
    mode: production
    variables:
      catalog:
        default: "citibike_prod"
    workspace:
      host: https://adb-3388160243615587.7.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/torranceross@ymail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/torranceross@ymail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: torranceross@ymail.com
        level: CAN_MANAGE
